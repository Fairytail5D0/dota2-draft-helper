# Copy this file to ".env" (without .example) and paste your Stratz token.
# The .env file is gitignored and must never be committed or shared.
# No token? Leave it empty — the script will run on OpenDota only.

STRATZ_TOKEN=
